---
layout: post
title: "Git"
subtitle: "Git"
date: 2022-06-05
author: "Lbb"
header-img: "images/post-bg.jpg"
tags: [ tools ]
---

Git 常用命令速查，按使用场景整理，覆盖安装、配置、仓库、文件操作、分支、版本回滚与排查。

## 安装

- Linux

```shell
# Fedora 上的 yum:
$ sudo yum install git
# Debian 上的 apt-get:
$ sudo apt-get install git
```

- Mac：安装 Xcode Command Line Tools（Mavericks 及以后），或从官网下载安装包
- Windows：从官网下载安装包
- 版本查询

```shell
git --version
```

## 环境设置

初次运行 Git 前需要配置 `user.name` 和 `user.email`。

Git 有三个配置文件（优先级从高到低）：

- 版本库级别：`/.git/config`
- 全局：用户主目录下的 `~/.gitconfig`
- 系统级：`/etc/gitconfig`

```shell
git config -e                # 编辑当前仓库配置
git config -e --global       # 编辑全局配置
git config -e --system       # 编辑系统配置
git config <section>.<key>   # 读取某个配置
git config <section>.<key> <value>  # 设置某个配置
```

- 设置用户名与邮箱

```shell
git config --global user.name "Your Name"
git config --global user.email "email@example.com"
```

- 查看配置

```shell
git config --list
```

- 避免 pull 时产生多余的合并提交（用 rebase 代替 merge）

```shell
git config --global pull.rebase true
```

- 颜色高亮

```shell
git config --global color.ui true
git config --global color.status auto
git config --global color.branch auto
```

- 设置默认编辑器

```shell
git config --global core.editor vim
```

- 设置命令别名

```shell
git config --global alias.ci commit
git config --global alias.co checkout
```

- 设置默认合并工具

```shell
git config --global merge.tool vimdiff
```

- 删除配置

```shell
git config --unset --global user.name
git config --unset --global user.email
```

- SSH 密钥：生成 RSA 密钥对，将公钥配置到 GitHub

```shell
ssh-keygen -t rsa -C "youremail@example.com"
cat ~/.ssh/id_rsa.pub
ssh -T git@github.com   # 验证是否连通
```

生成的一对密钥中，`id_rsa` 是私钥（保密），`id_rsa.pub` 是公钥（填到 GitHub）。

## 仓库

- 创建本地版本库

```shell
mkdir gittest
cd gittest
git init
```

- 克隆远程仓库；`--bare` 和 `--mirror` 创建不含工作区的裸版本库

```shell
git clone <repository> <directory>
git clone --bare <repository> <directory.git>
git clone --mirror <repository> <directory.git>
```

- 关联远程仓库

```shell
git remote add origin git@github.com:lbb4511/Story.git
git push -u origin master   # 首次推送并设置上游跟踪
```

- 拉取 / 推送

```shell
git pull origin master    # 从远程拉取并合并到当前分支
git fetch origin          # 只拉取远程更新，不合并
git push origin master    # 推送本地提交到远程
git push --tags           # 推送所有标签
```

- 推送被拒绝：原因是远程仓库和本地不同步，需先拉取合并再推送

```shell
git fetch origin
git merge origin/master
git push origin master
```

更简单的做法是直接 `git pull origin master`（等价于 fetch + merge）。

## 文件操作

- 添加（git add）

```shell
git add <file>    # 添加文件到暂存区
git add .         # 递归添加当前目录所有文件
git add -u        # 添加已跟踪文件的改动（含删除和修改）
git add -A        # 添加所有改动（已跟踪 + 未跟踪）
git add -i        # 交互式添加
```

- 删除（git rm）

`rm` 只删除工作区文件；`git rm` 会把删除动作加入暂存区，提交后真正删除文件。

```shell
git rm <file>     # 删除文件
git rm -r <dir>   # 递归删除目录
```

- 移动或重命名（git mv）

```shell
git mv <old> <new>
```

- 清除（git clean）：删除本地多余的未跟踪文件与目录

```shell
git clean -nd     # 预删除，显示将要删除的内容
git clean -fd     # 强制删除多余的文件和目录
```

- 查看状态（git status）

```shell
git status      # 查看工作区与暂存区状态
git status -s   # 简短输出
```

- 查看差异（git diff）

![git diff]({{ site.baseurl }}/images/posts/git-diff.webp)

```shell
git diff              # 工作区与暂存区差异
git diff <file>       # 查看某文件本次修改内容
git diff HEAD         # 工作区与 HEAD 的差异
git diff --cached     # 暂存区与 HEAD 的差异
git diff <id1> <id2>  # 两次提交之间的差异
```

- 提交（git commit）

```shell
git commit -m "提交描述"      # 提交
git commit -am "提交描述"     # add + commit 一步完成
git commit --amend           # 修补上一次提交
git commit --allow-empty     # 允许空提交
```

- 查看历史（git log）

```shell
git log --oneline             # 简短显示提交
git log --graph               # 图示分支与合并
git log --pretty=raw          # 显示 parent 属性
git log --stat                # 显示每个提交涉及的变动文件
git log -5                    # 最近 5 条
git log -p                    # 显示每个提交的完整差异
git show <commit>             # 查看某次提交详情
git show HEAD@{5}             # 查看当前分支第 5 次之前的状态
```

## 分支

- 查看 / 创建 / 删除

```shell
git branch                    # 列出本地分支
git branch -a                 # 列出所有分支（含远程）
git branch <name>             # 创建分支
git branch <name> <commit>    # 基于指定提交创建分支
git branch -d <name>          # 删除分支（已合并才允许）
git branch -D <name>          # 强制删除分支
git branch -m <old> <new>     # 重命名分支
git branch --merged           # 显示已合并到当前分支的分支
git branch --no-merged        # 显示未合并到当前分支的分支
```

- 切换分支（传统写法）

```shell
git checkout <branch>      # 切换分支
git checkout -b <branch>   # 创建并切换分支
```

- 切换分支（现代写法，语义更清晰）

```shell
git switch <branch>        # 切换分支
git switch -c <branch>     # 创建并切换分支
```

- 合并（git merge）

```shell
git merge <branch>          # 合并分支到当前分支
git merge origin/master     # 合并远程 master 到当前分支
git cherry-pick <commit>    # 挑选某次提交应用到当前分支
```

- 变基（git rebase）

![git rebase]({{ site.baseurl }}/images/posts/git-rebase.webp)

```shell
git rebase <branch>      # 将当前分支变基到目标分支
git rebase -i <commit>   # 交互式变基（合并、改写提交）
```

## 回滚与版本控制

- 撤销工作区改动（git restore，现代写法）

```shell
git restore <file>                 # 用暂存区覆盖工作区
git restore --staged <file>        # 把文件撤出暂存区
git restore --source=<commit> <file>  # 从指定提交恢复文件
```

- 传统写法（git checkout --）

```shell
git checkout -- <file>     # 用暂存区覆盖工作区文件
git checkout <commit> -- <file>  # 用指定提交覆盖暂存区与工作区
git checkout .             # 丢弃所有本地修改（危险操作）
```

- 版本回退（git reset）

![git reset]({{ site.baseurl }}/images/posts/git-reset.webp)

```shell
git reset --hard <commit>   # 移动 HEAD，重置暂存区和工作区（危险，会丢改动）
git reset --soft <commit>   # 只移动 HEAD，不动暂存区和工作区
git reset --mixed <commit>  # 默认：移动 HEAD 并重置暂存区，保留工作区
git reset HEAD <file>       # 将文件撤出暂存区
```

提交 ID 可以使用完整 SHA-1，也可以只写前几位。

- 撤销提交（git revert）：新建一个反向提交，保留历史，适合已推送的分支

```shell
git revert <commit>
```

- 找回提交（git reflog）：显示所有 HEAD 移动记录，可找回被 reset 掉的提交

```shell
git reflog
```

- 标签（git tag）

```shell
git tag                    # 列出标签
git tag -a v2.0 -m '描述'  # 打附注标签
git show v2.0              # 查看标签详情
git push --tags            # 推送所有标签
```

## 其他常用命令

- 暂存（git stash）：把当前未提交的修改暂存起来，工作区回到干净状态

```shell
git stash                 # 暂存当前修改
git stash list            # 查看暂存列表
git stash show -p stash@{0}  # 查看某次暂存内容
git stash apply stash@{0}    # 应用暂存
git stash pop             # 应用并删除暂存
```

- 检索（git grep）：搜索文件内容

```shell
git grep 'text'           # 在工作区中搜索
git grep 'text' v1.0      # 在指定版本中搜索
```

- 归档（git archive）：把某次提交的目录树打包

```shell
git archive -o latest.zip HEAD
git archive -o partial.tar HEAD src doc
git archive --format=tar --prefix=1.0/ v1.0 | gzip > foo-1.0.tar.gz
```

- 查看 Git 对象（git cat-file）

```shell
git cat-file -t <object>   # 查看对象类型（tree、commit、blob 等）
git cat-file -p <object>   # 查看对象内容
```

- 排查仓库（git fsck / git blame）

```shell
git fsck              # 检查仓库完整性
git blame <file>      # 查看文件每行最后修改的提交
```

## 命令速查表

| 命令                                                      | 注释                                                                |
| --------------------------------------------------------- | ------------------------------------------------------------------- |
| git init                                                  | 初始化本地 git 仓库（创建新仓库）                                   |
| git config --global user.name "xxx"                       | 配置用户名                                                          |
| git config --global user.email "xxx@xxx.com"              | 配置邮件                                                            |
| git config --global color.ui true                         | git status 等命令自动着色                                           |
| git clone git+ssh://git@192.168.53.168/VT.git             | clone 远程仓库                                                      |
| git status                                                | 查看当前版本状态（是否修改）                                        |
| git add xyz                                               | 添加 xyz 文件至 index                                               |
| git add .                                                 | 增加当前子目录下所有更改过的文件至 index                            |
| git commit -m 'xxx'                                       | 提交                                                                |
| git commit --amend -m 'xxx'                               | 合并上一次提交（用于反复修改）                                      |
| git commit -am 'xxx'                                      | 将 add 和 commit 合为一步                                           |
| git rm xxx                                                | 删除 index 中的文件                                                 |
| git rm -r \*                                              | 递归删除                                                            |
| git log                                                   | 显示提交日志                                                        |
| git log -1                                                | 显示 1 行日志，-n 为 n 行                                           |
| git log --stat                                            | 显示提交日志及相关变动文件                                          |
| git log -p -m                                             | 显示完整差异                                                        |
| git show dfb02e6e4f2f7b573337763e5c0013802e392818         | 显示某个提交的详细内容                                              |
| git show dfb02                                            | 可只用 commitid 的前几位                                            |
| git show HEAD                                             | 显示 HEAD 提交日志                                                  |
| git show HEAD^                                            | 显示 HEAD 的父（上一个版本）的提交日志，^^ 为上两个版本             |
| git tag                                                   | 显示已存在的 tag                                                    |
| git tag -a v2.0 -m 'xxx'                                  | 增加 v2.0 的 tag                                                    |
| git show v2.0                                             | 显示 v2.0 的日志及详细内容                                          |
| git log v2.0                                              | 显示 v2.0 的日志                                                    |
| git diff                                                  | 显示所有未添加至 index 的变更                                       |
| git diff --cached                                         | 显示所有已添加 index 但还未 commit 的变更                           |
| git diff HEAD^                                            | 比较与上一个版本的差异                                              |
| git diff HEAD -- ./lib                                    | 比较与 HEAD 版本 lib 目录的差异                                     |
| git diff origin/master..master                            | 比较远程分支 master 上有本地分支 master 上没有的                    |
| git remote add origin git+ssh://git@192.168.53.168/VT.git | 增加远程定义（用于 push/pull/fetch）                                |
| git branch                                                | 显示本地分支                                                        |
| git branch --contains 50089                               | 显示包含提交 50089 的分支                                           |
| git branch -a                                             | 显示所有分支                                                        |
| git branch -r                                             | 显示所有远程分支                                                    |
| git branch --merged                                       | 显示所有已合并到当前分支的分支                                      |
| git branch --no-merged                                    | 显示所有未合并到当前分支的分支                                      |
| git branch -m master master_copy                          | 本地分支改名                                                        |
| git checkout -b master_copy                               | 从当前分支创建新分支 master_copy 并检出                             |
| git checkout -b master master_copy                        | 上面的完整版                                                        |
| git checkout features/performance                         | 检出已存在的 features/performance 分支                              |
| git checkout --track hotfixes/BJVEP933                    | 检出远程分支 hotfixes/BJVEP933 并创建本地跟踪分支                   |
| git checkout v2.0                                         | 检出版本 v2.0                                                      |
| git checkout -b devel origin/develop                      | 从远程分支 develop 创建新本地分支 devel 并检出                      |
| git checkout -- README                                    | 检出 HEAD 版本的 README 文件（可用于修改错误回退）                  |
| git merge origin/master                                   | 合并远程 master 分支至当前分支                                      |
| git cherry-pick ff44785404a8e                             | 合并提交 ff44785404a8e 的修改                                       |
| git push origin master                                    | 将当前分支 push 到远程 master 分支                                  |
| git push origin :hotfixes/BJVEP933                        | 删除远程仓库的 hotfixes/BJVEP933 分支                               |
| git push --tags                                           | 把所有 tag 推送到远程仓库                                          |
| git fetch                                                 | 获取所有远程分支（不更新本地分支，另需 merge）                      |
| git fetch --prune                                         | 获取所有远程分支并清除服务器上已删掉的分支                          |
| git pull origin master                                    | 获取远程分支 master 并 merge 到当前分支                             |
| git mv README README2                                     | 重命名文件 README 为 README2                                        |
| git reset --hard HEAD                                     | 将当前版本重置为 HEAD（通常用于 merge 失败回退）                    |
| git rebase                                                | 变基当前分支                                                        |
| git branch -d hotfixes/BJVEP933                           | 删除分支 hotfixes/BJVEP933（已合并到其他分支）                      |
| git branch -D hotfixes/BJVEP933                           | 强制删除分支 hotfixes/BJVEP933                                      |
| git ls-files                                              | 列出 git index 包含的文件                                           |
| git show-branch                                           | 图示当前分支历史                                                    |
| git show-branch --all                                     | 图示所有分支历史                                                    |
| git whatchanged                                           | 显示提交历史对应的文件修改                                          |
| git revert dfb02e6e4f2f7b573337763e5c0013802e392818       | 撤销提交 dfb02e6e4f2f7b573337763e5c0013802e392818                   |
| git ls-tree HEAD                                          | 内部命令：显示某个 git 对象                                        |
| git rev-parse v2.0                                        | 内部命令：显示某个 ref 对应的 SHA1 HASH                             |
| git reflog                                                | 显示所有提交，包括孤立节点                                          |
| git show master@{yesterday}                               | 显示 master 分支昨天的状态                                          |
| git log --pretty=format:'%h %s' --graph                   | 图示提交日志                                                        |
| git stash                                                 | 暂存当前修改，将所有至为 HEAD 状态                                  |
| git stash list                                            | 查看所有暂存                                                      |
| git stash show -p stash@{0}                               | 参考第一次暂存                                                    |
| git stash apply stash@{0}                                 | 应用第一次暂存                                                    |
| git grep "delete from"                                    | 文件中搜索文本 delete from                                          |
| git gc                                                    | 压缩仓库，垃圾回收                                                  |
| git fsck                                                  | 检查仓库完整性                                                      |
